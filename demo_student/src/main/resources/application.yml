server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:23306/springboot_study?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: attust123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.attust.demo_student.entity